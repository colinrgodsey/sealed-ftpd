# 2. Database Schema

Design and implement the SQLite schema to represent the file system structure (files, directories, metadata).

## Testing

Unit tests will be written to verify that the database schema is created correctly and that any initial data is inserted as expected.
