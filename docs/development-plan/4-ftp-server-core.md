# 4. FTP Server Core

Implement the basic FTP server, including handling connections, commands, and responses.

## Testing

Unit tests will be written to verify that the FTP server can handle basic connections, commands, and responses correctly.
