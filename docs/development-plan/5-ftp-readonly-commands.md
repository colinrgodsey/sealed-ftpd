# 5. Read-Only FTP Commands

Implement read-only FTP commands like `LIST`, `NLST`, `CWD`, `PWD`, `TYPE`, `SIZE`, `RETR`.

## Testing

Unit tests will be written for each read-only FTP command (`LIST`, `NLST`, `CWD`, `PWD`, `TYPE`, `SIZE`, `RETR`) to ensure they function as expected.
