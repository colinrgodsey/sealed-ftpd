# 1. Project Setup and Dependencies

Create a local git repository for the project. Each integration step will be
a new commit in the repo.

Initialize the Go project and add the necessary dependencies for SQLite and FTP.
